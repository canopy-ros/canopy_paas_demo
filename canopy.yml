before_install:
    - echo "Before Install"

ros_install:
    - robot-localization

install:
    - sudo apt-get install ipython

before_script:
    - echo "Before Script"

script:
    - catkin_make

after_script:
    - echo "After Script"

before_deploy:
    - echo "Before Deploy"

deploy:
    - roslaunch canopy_paas_test leaflet.launch host:=$HOST port:=$PORT key:=$KEY container_name:=$NAME

after_deploy:
    - echo "After Deploy"
